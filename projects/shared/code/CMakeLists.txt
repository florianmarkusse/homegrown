cmake_minimum_required(VERSION 3.22)
project(shared LANGUAGES C ASM)

if(CMAKE_SOURCE_DIR STREQUAL PROJECT_SOURCE_DIR)
    include(${REPO_PROJECTS}/setup.cmake)
    include(${REPO_PROJECTS}/print-configuration.cmake)
    add_link_options(--ld-path=${CMAKE_LINKER})
endif()

add_subdirectory_from_root(assert)
add_subdirectory_from_root(text)
add_subdirectory_from_root(memory)
add_subdirectory_from_root(log)
add_subdirectory_from_root(maths)
add_subdirectory_from_root(buffer)
add_subdirectory_from_root(hash)
add_subdirectory_from_root(dynamic-array)
add_subdirectory_from_root(types)
add_subdirectory_from_root(macros)
